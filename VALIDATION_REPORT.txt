ANA TAGES — CLEAN START v2.0.2

Origem da correção:
- Build Cloudflare de 24/09/2026.
- Instalação de dependências concluída.
- verify:project concluído.
- Vite client build concluído.
- Vite SSR build concluído.
- A falha ocorreu exclusivamente no passo `tsc --noEmit`.

Erros do typecheck tratados nesta versão:
1. TS4111 em dataset do Turnstile -> acesso por índice.
2. TS18047 no Supabase dentro de callbacks -> cliente não nulo capturado em constante local.
3. TS4111 nos objetos `allowed` de repository.ts -> acessos por índice.
4. TS4111 nas variáveis `import.meta.env` -> acessos por índice.
5. TS2345 / TS18048 no parsing de mês -> parsing validado sem valores undefined.

Validações locais possíveis neste ambiente:
- `node scripts/verify-project.mjs`: OK.
- Varredura de imports locais/dependências: OK pelo verificador do projeto.
- Nenhum node_modules/package-lock antigo foi incluído no pacote.

Limitação do ambiente desta geração:
- O registro npm não estava acessível (EAI_AGAIN), portanto não foi possível reinstalar as dependências localmente para repetir o build completo fora da Cloudflare.
- O log da Cloudflare demonstrou que a instalação e os dois builds Vite da v2.0.1 já passavam; as mudanças v2.0.2 são restritas aos erros de TypeScript listados pelo próprio `tsc`.
